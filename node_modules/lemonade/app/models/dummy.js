/**
 * Dummy model
 * @type {Function}
 */
function DummyModel() {

    // All objects have access to :
    this.kernel;
    // and so to all kernel links defined in config.js

    // All objects included via an instance object
    // have access to the instance :
    this.instance;
}

/**
 * Exports
 */
exports = module.exports = DummyModel;